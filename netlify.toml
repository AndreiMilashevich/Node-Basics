[build]
publish = "build"
functions = "api"

[functions]
external_node_modules = ["express", "mongoose", "serverless-http", "method-override", "ejs", "dotenv"]
node_bundler = "esbuild"

[[redirects]]
force = true
from = "/*"
status = 200
to = "/.netlify/functions/app/:splat"